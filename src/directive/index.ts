import { Badge } from './Badge';
import { Shown } from './Shown';
import { Tooltip, TooltipSide } from './Tooltip';
import { Background as Bg, BackgroundColors } from './Background';

// Directives
export default {
  Bg,
  Badge,
  Shown,
  Tooltip,
};

export {
  Badge,
  Bg,
  BackgroundColors,
  Tooltip,
  TooltipSide,
};
