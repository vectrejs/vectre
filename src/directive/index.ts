import { Badge } from './Badge';
import { Loading } from './Loading';
import { Tooltip, TooltipSide } from './Tooltip';

// Directives
export default {
  Badge,
  Loading,
  Tooltip,
};

export {
  Badge,
  Loading,
  Tooltip,
  TooltipSide,
};
