export type Color = keyof typeof Colors;

export enum Colors {
  primary = 'bg-primary',
  secondary = 'bg-secondary',
  dark = 'bg-dark',
  gray = 'bg-gray',
  success = 'bg-success',
  warning = 'bg-warning',
  error = 'bg-error',
}
