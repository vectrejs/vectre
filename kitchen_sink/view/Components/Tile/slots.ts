import { SlotDefinitions } from '@kitchen/component/Slots';

export const slots: SlotDefinitions = {
  default: { description: 'Custom content shown after subtitle in the content section' },
  actions: { description: 'Actions section' },
  icon: { description: 'Adds the ability to display content in the icon section' },
};
